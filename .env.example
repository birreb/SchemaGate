# Copy to .env and fill in. Never commit .env.

# Named database connections, as JSON. The key is what callers pass as `connection`.
SCHEMAGATE_CONNECTIONS={"primary": "postgresql://user:password@localhost:5432/mydb"}

# Maximum accepted upload size in bytes. Enforced before the body is read.
SCHEMAGATE_MAX_UPLOAD_BYTES=10485760
